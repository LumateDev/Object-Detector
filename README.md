# 🎯 Object Detector v1.0

Приложение для детекции объектов в реальном времени с использованием веб-камеры ноутбука и нейросети YOLOv8.

## ✨ Возможности

- 🔍 Детекция трёх типов объектов: человек, чашка, телефон
- 📹 Работа с веб-камерой в реальном времени
- ⚡ Оптимизация для работы на CPU
- 📊 Отображение статистики и FPS
- 📷 Сохранение скриншотов
- ⚙️ Гибкая настройка через конфиг

## 🚀 Быстрый старт

### 1. Создание виртуального окружения

Откройте терминал (например, PowerShell или Command Prompt) в папке проекта и выполните:

```bash
# Создание виртуального окружения
python -m venv venv

# Активация виртуального окружения
# Windows:
venv\Scripts\activate
# Linux/macOS:
source venv/bin/activate
```

После активации в начале строки в терминале должен появиться указатель `(venv)`.

### 2. Установка зависимостей

Убедитесь, что виртуальное окружение активировано, и выполните:

```bash
pip install -r requirements.txt
```

> **Примечание для пользователей GPU (NVIDIA):** Если вы хотите использовать GPU для ускорения, замените `requirements.txt` на команду:
>
> ```bash
> pip install ultralytics opencv-python numpy PyYAML torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
> ```
>
> (Или соответствующий индекс для вашей версии CUDA.)

### 3. Запуск

```bash
python main.py
```

При первом запуске модель YOLOv8n будет загружена автоматически.

## ⌨️ Управление

| Клавиша     | Действие                             |
| ----------- | ------------------------------------ |
| `ПРОБЕЛ`    | Старт/Пауза детекции                 |
| `S`         | Сохранить скриншот                   |
| `F`         | Показать/скрыть FPS                  |
| `C`         | Показать/скрыть проценты уверенности |
| `I`         | Показать/скрыть панель статистики    |
| `Q` / `ESC` | Выход                                |

## ⚙️ Режимы работы

```bash
python main.py --fast

python main.py --balanced

python main.py --accurate

python main.py --very-accurate

python main.py --ultra-accurate

python main.py --high-res-accurate

python main.py --high-conf-accurate
```

## 📁 Структура проекта

```
object_detector/
├── main.py           # Основной скрипт
├── requirements.txt  # Зависимости
├── config.yaml       # Настройки
├── screenshots/      # Скриншоты (создаётся автоматически)
└── README.md         # Документация
```

## 🔧 Настройка

Отредактируйте `config.yaml` для изменения параметров:

- Разрешение камеры
- Порог уверенности
- Отображаемые элементы интерфейса

## 📋 Требования

- Python 3.8+
- Веб-камера
- 4 GB RAM (рекомендуется 8 GB)
- Интернет (для первой загрузки модели)

## 🐛 Решение проблем

**Камера не найдена:**

```bash
python main.py --camera 1
```

**Низкий FPS:**

```bash
python main.py --fast
```

**Много ложных срабатываний:**

```bash
python main.py --confidence 0.7
```

## 🚀 Запуск

```bash
# 1. Создайте и активируйте виртуальное окружение (см. выше)
# 2. Установите зависимости
pip install -r requirements.txt

# 3. Базовый запуск
python main.py

# С настройками
python main.py --fast              # Быстрый режим
python main.py --accurate          # Точный режим
python main.py --camera 1          # Другая камера
python main.py --confidence 0.7    # Высокий порог
```
